# Pusher Channels credentials
PUSHER_APP_ID=""
PUSHER_KEY=""
PUSHER_SECRET=""
PUSHER_CLUSTER="mt1"

# Expose the key and cluster to the browser
NEXT_PUBLIC_PUSHER_KEY=""
NEXT_PUBLIC_PUSHER_CLUSTER="mt1"

# Restrict Netlify Function access (optional)
APP_ALLOWED_ORIGIN="http://localhost:3000"

# Persistent store (recommended for Netlify deploys)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
